# 📈 因子投資系統 Factor Investing System

## 🧭 專案介紹

本專案為一個模組化的「**因子投資系統**」，整合了因子資料整理、報酬分析、視覺化與投組績效評估功能。  
透過自動化的資料匯入與分析流程，使用者可快速完成以下工作：

- 📊 匯入與更新因子資料（自動整併舊檔與新資料）  
- ⚙️ 依時期生成完整因子報酬績效報表  
- 🔥 分析各因子近期表現、視覺化比較結果  
- 📅 查詢當期投資組合持股清單 
- 💡 分析投組中最具貢獻的個股與報酬分布  

本系統適用於**台股量化投資策略研究、因子有效性測試、或投組績效歸因分析**。

---

## 🗂 專案架構

```
📁 專案目錄
│
├── merged_csvs/               # 更新後的因子資料集（自動輸出資料夾）
│
├── __pycache__/               # Python 快取（自動生成）
│
├── alpha.py                   # 因子邏輯函數（Alpha 訊號生成）
├── clean_data.py              # 資料清理函數
├── eps_growth.py              # EPS 成長率因子邏輯
├── import_lab.py              # 匯入與資料處理工具
├── interval_report.py         # 報表生成與可視化函數
├── pe.py                      # 本益比因子邏輯
├── portfo_analysis.py         # 投組績效與歸因分析
├── revenue_growth.py          # 營收成長率因子邏輯
├── yeild.py                   # 殖利率因子邏輯
│
├── main.ipynb                 # 主 Notebook：因子運算與函數應用
├── update_data.ipynb          # 更新資料時使用的 Notebook
│
├── 因子資料全.xlsx             # 2025-08-26 以前的資料
├── 更新因子.xlsx               # 2025-08-26 之後資料（手動從 Cmoney 更新）
│
└── README.md                  # 專案說明文件（本檔案）
```

---

## 🚀 整體操作流程

1️⃣ 手動更新 `更新因子.xlsx`（從 Cmoney 匯入最新資料）  
⬇️  
2️⃣ 執行 `update_data.ipynb` =>runall（將新資料整併至 `merged_csvs/`）  
⬇️  
3️⃣ 開啟 `main.ipynb`（主要操作平台）  
⬇️  
4️⃣ 匯入並定義主要因子（如 `pe`, `revenue_growth`, `eps_growth`, `yeild`）  
⬇️  
5️⃣ 使用下列四個核心函數進行分析與視覺化  

---

# ⚙️ 函數說明與使用範例

---

## 函數1：指定區間因子報酬報表  
（`perf_report`）

**功能**  
比較多個投資組合或因子在指定期間內的績效，輸出：
- 累積報酬曲線  
- Sharpe Ratio / 年化報酬比較圖  
- 績效總表（DataFrame）

**定義**
```python
def perf_report(returns_dict: dict, start_date: str, end_date: str, 
                freq: int = 252, rf: float = 0.0)
```

**參數**
| 參數 | 型態 | 說明 |
|------|------|------|
| `returns_dict` | dict[str, pd.Series] | 每個因子或策略的日報酬率 |
| `start_date`, `end_date` | str | 分析區間 |
| `freq` | int | 年化基數（台股預設 252） |
| `rf` | float | 無風險利率（預設 0） |

**輸出**
- 圖表1️⃣：累積報酬曲線  
- 圖表2️⃣：Sharpe Ratio 長條圖  
- DataFrame：各策略績效表（含年化報酬、波動、Sharpe、總報酬）

**範例**
```python
returns_dict = {
    "PE_Low": pe_low_returns,
    "PB_Low": pb_low_returns,
    "Momentum": mom_returns
}
perf_table = perf_report(returns_dict, "2024-01-01", "2024-12-31")
```

---

## 函數2：最近一年因子月表現熱力圖  
（`factor_rank_tile_full`）

**功能**  
顯示各因子在每期（月、週、年）中的單期報酬排名，  
以**熱力圖形式**可視化因子強弱與穩定性。

**定義**
```python
def factor_rank_tile_full(
    returns_dict: dict,
    end_date: str | None = None,
    periods: int = 12,
    freq: str = "M",
    title: str = "Factor Ranking Tile Chart",
    save_html: str | None = None
)
```

**參數**
| 參數 | 型態 | 說明 |
|------|------|------|
| `returns_dict` | dict[str, pd.Series] | 各因子報酬率 |
| `end_date` | str | 結束日期（自動抓取最大日期） |
| `periods` | int | 顯示最近 N 期（預設 12） |
| `freq` | str | 週期："M" 月 / "W" 週 / "Y" 年 |
| `title` | str | 圖表標題 |
| `save_html` | str | 儲存 HTML 檔路徑（可選） |

**輸出**
- 方格圖（Plotly）：顯示因子名、報酬%與當期排名  
- DataFrame：各期報酬與排名結果  

**範例**
```python
factor_rank_tile_full(
    returns_dict=factor_returns_dict,
    end_date="2025-09-30",
    periods=12,
    freq="M"
)
```

---

## 函數3：當日持股明細  
（`porfolio_list`）

**功能**  
根據因子選股矩陣（0/1）查詢某日期的持股清單。

**定義**
```python
def porfolio_list(alpha, date):
    return alpha.columns[(alpha.loc[date] == 1)]
```

**參數**
| 參數 | 型態 | 說明 |
|------|------|------|
| `alpha` | pd.DataFrame | 選股訊號矩陣（index=日期, columns=股票代號, 值=0/1） |
| `date` | str | 指定日期（`YYYY-MM-DD`） |

**輸出**
- `pd.Index`：該日應持有股票清單  

**範例**
```python
tickers = porfolio_list(pe_low_01_alpha, "2025-01-20")
print(tickers)
# ➜ Index(['1101', '1210', '2317', ...], dtype='object')
```

---

## 函數4：投資組合貢獻最多的個股  


（`porfolio_list_period`）

## 🧩 功能說明
此函數用於找出在指定期間內最常被選入投資組合（alpha矩陣中為 1 次數最多）的股票，
並根據這些股票生成投資組合貢獻報表。  
可用於觀察「長期被選中」的核心持股表現與貢獻。

---

## 🧠 函數定義
```python
def porfolio_list_period(alpha, start, end, num=30):
    selected_days = alpha.loc[start:end].sum(axis=0)
    t = selected_days.sort_values(ascending=False).head(num).index
    tickers = t
    stats_df, port_ret = top_contributors_report(
        returns, tickers, start_date=start, end_date=end, top_n=10
    )
    return stats_df
```

---

## ⚙️ 參數說明

| 參數 | 型態 | 說明 |
|------|------|------|
| `alpha` | pd.DataFrame | 選股訊號矩陣（index=日期, columns=股票代號, 值=0/1） |
| `start`, `end` | str | 指定分析期間（格式：`YYYY-MM-DD`） |
| `num` | int | 取出在期間內出現次數最多的前 N 檔股票（預設 30） |

---

## 📤 輸出結果

- `pd.DataFrame`：`top_contributors_report()` 產生的報表，包含：  
  - `Annual Return`（年化報酬）  
  - `Annual Volatility`（年化波動）  
  - `Sharpe Ratio`（夏普比率）  
  - `Period Return`（區間報酬）  
  - `Contribution`（對投組貢獻）  

---

## 💡 使用範例

```python
# 分析 2024 全年度中最常被 alpha 訊號選入的前 30 檔股票
stats_df = porfolio_list_period(
    alpha=pe_low_01_alpha,
    start="2024-01-01",
    end="2024-12-31",
    num=30
)
print(stats_df.head(10))
```
